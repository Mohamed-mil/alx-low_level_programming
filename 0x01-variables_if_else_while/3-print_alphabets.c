#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 (success)
*/

int main(void)
{
	char h = 'a';
	char H = 'A';

	while (ch <= 'z')
	{
		putchar(h);
		h++;
	}
	while (H <= 'Z')
	{
		putchar(H);
		H++;
	}
	putchar('\n');

	return (0);
}
