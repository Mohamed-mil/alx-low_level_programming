#include "main.h"

/**
 * _strncat - concatenating two strings
 *
 * @dest: string
 * @src: string
 * @n: character
 * Return: dest
*/

char *_strncat(char *dest, char *src, int n);
{
	char dest[] = "do this";
	char src[7] = "do that";
	int n = 7;

	printf("%s\n", strncat(dest, src, n))

			return (dest);
}
