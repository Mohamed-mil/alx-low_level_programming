#include "main.h"

/**
 * *_strcat - concatenating two strings
 *
 * @dest: string
 * @src: string
 * Return: aways 0
*/

char *_strcat(char *dest, char *src)
{
	char dest[] = "Ballon";
	char src[] = "Dor";

	printf("the winner for%s\n", strcat(s1, s2));

	return (0);
}
